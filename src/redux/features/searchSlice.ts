import { createSlice } from "@reduxjs/toolkit";




const initialState={};


const searchSalonSlice = createSlice({
    name:'search',
    initialState,
    reducers:{}
});

export const {} = searchSalonSlice.actions
export default searchSalonSlice.reducer